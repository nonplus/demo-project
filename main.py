def move(current_row, current_col, direction):
  # Some code
  return [ current_row, current_col ]

[ player_row, player_col ] = move(1, 2, 'up')

print(f'New row is {player_row}')
print(f'New col is {player_col}')